# TBD

## GM server 실행 guide
- node.js 설치 후 console의 server directory에서  다음 명령어 실행 cmd.exe /c "npm install && npm start" 

## Game node 실행 guide
- 서버와 모든 노드 기기를 허브에 이더넷으로 연결(linux ip 변경 필요)
- Ubuntu 연결 후 cd /mnt/nfs, ./game_node 실행 시 서버와 연결됨
- ending directory의 image 또한 nfs로 복사
- 
